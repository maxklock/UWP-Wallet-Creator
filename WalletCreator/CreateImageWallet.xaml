<Page
    x:Class="WalletCreator.CreateImageWallet"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WalletCreator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="20">
            <Button x:Name="btnPhoto"
                    Content="Take Photo"
                    HorizontalAlignment="Stretch" Click="BtnPhotoClick" />
            <local:CropImageControl x:Name="cropImage"
                                            Margin="0,20,0,0" />
            <Button x:Name="btnCrop"
                    Margin="0,20,0,0"
                    Content="Crop"
                    HorizontalAlignment="Stretch" Click="BtnCropClick" />
            <Button x:Name="btnRotate"
                    Margin="0,20,0,0"
                    Content="Rotate"
                    HorizontalAlignment="Stretch" Click="BtnRotateClick" />
            <Button x:Name="btnWallet"
                    Margin="0,20,0,0"
                    Content="Add To Wallet"
                    HorizontalAlignment="Stretch" Click="BtnWalletClick" />
        </StackPanel>
    </ScrollViewer>
</Page>
